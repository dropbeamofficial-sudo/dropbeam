{
  "name": "dropbeam",
  "version": "1.0.0",
  "description": "Instant peer-to-peer file transfer with 6-digit codes and QR",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "deploy:functions": "npx supabase functions deploy init-upload && npx supabase functions deploy confirm-upload && npx supabase functions deploy get-download && npx supabase functions deploy file-info && npx supabase functions deploy stats && npx supabase functions deploy admin-transfers"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.22.1",
    "socket.io": "^4.8.3",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express-rate-limit": "^7.5.1",
    "multer": "^1.4.5-lts.2",
    "archiver": "^5.3.2"
  }
}
